class Reservation {
    constructor(Id, VenueId, GuestId, ReservationDateTime, HourCount) {
      this.Id = Id;
      this.VenueId = VenueId;
      this.GuestId = GuestId;
      this.ReservationDateTime = ReservationDateTime;
      this.HourCount = HourCount;
    }
}